{
  "name": "arch-browser-history",
  "version": "0.1.4",
  "description": "Obsidian plugin that writes browser history into day notes, with repeats folded and adult sites removed.",
  "private": true,
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
